/**
 * Dossiers sous assets/video/ référencés par le fallback statique (projects.ts).
 * Indépendant de assetMediaMap.ts (généré) : le build Vercel peut générer une map vide.
 */
export type ProjectAssetFolder = 'app_mobile' | 'esct' | 'site_mariage_1' | 'vodason_compagn';
